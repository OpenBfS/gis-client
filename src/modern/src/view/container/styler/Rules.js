/**
 * This class only exists to satisfy the compiler.
 *
 * TODO we need to check where in `app` we require this view and probably change
 *      this.
 *
 * @class Koala.view.container.styler.StyleEditor
 */
Ext.define("Koala.view.container.styler.StyleEditor", {
    extend: "Ext.Component"
});
